#!/bin/bash

# shellcheck disable=SC1091
source /usr/lib/ore/ore-init

readonly config_file="$ORE_HOME/config.yaml"

if [[ ! -f $config_file ]]; then
  mkdir -p "$(dirname "$config_file")"
  touch "$config_file"
fi

${EDITOR:-vi} "$config_file"
